[project]
name = "kong-jwt-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyjwt>=2.13.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
kong-jwt-proxy = "kong_jwt_proxy:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
